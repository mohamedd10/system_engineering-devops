#!/usr/bin/env bash
# Script to terminate the 4-to_infinity_and_beyond process

# العثور على PID لكل عملية تعمل بالاسم
pid=$(pgrep -f "4-to_infinity_and_beyond")

# إذا وُجد PID، قم بقتل العملية
if [ -n "$pid" ]; then
    kill "$pid"
fi
